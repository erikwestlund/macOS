#!/usr/bin/env bash

set -euo pipefail

osascript <<'EOF'
tell application "Finder"
  set newWindow to make new Finder window
  set target of newWindow to (path to home folder as alias)
  activate
end tell
EOF
